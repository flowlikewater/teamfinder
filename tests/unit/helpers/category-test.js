
import { category } from 'teamfinder/helpers/category';
import { module, test } from 'qunit';

module('Unit | Helper | category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = category([42]);
  assert.ok(result);
});

